name: Secrets Access
on:
  workflow_dispatch



jobs:
  read-secret:
    runs-on: ubuntu-latest
    steps:
      - names: read secret
        if: ${{ secrets.MY_SECRET_MESSAGE }}
        run: echo "The secret is set: true" && echo "$secrets.MY_SECRET_MESSAGE"

