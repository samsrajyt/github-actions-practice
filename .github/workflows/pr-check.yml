name: pr-check

on:
  pull_request:
    branches: [main]



jobs:
  pr-check:
    runs-on: ubuntu-latest

    steps:
      - name: PR Check
        run: echo "PR check running for branch:${{ github.head_ref }}"
        


